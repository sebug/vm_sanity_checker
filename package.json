{
  "name": "vm_sanity_checker",
  "version": "1.0.0",
  "description": "Uses Acorn to parse & then traverses the syntax tree of view models to ensure they take the arguments the router provides.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sebastian Gfeller",
  "license": "MIT"
}
